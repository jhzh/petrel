git checkout
git branch
git push
git pull
 
删除分支：
git rm -r --cached a/2.txt                    // 删除a目录下的2.txt文件 

git commit -m  "删除a目录下的2.txt文件"  // commit

git push                               
